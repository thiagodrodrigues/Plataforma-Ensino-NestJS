export default {
    USERS: {
        MESSAGES: {
            ERROR: {
                USER_UNAUTHENTICATED: `Email ou Senha inválido. Usuário não autenticado`,
                USER_NOT_FOUND: `Usuário {USER_ID} não encontrado`
            }
        }
    }
}